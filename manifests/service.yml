apiVersion: v1
kind: Service
metadata:
    name: "myshuttle"
    labels:
        app: "myshuttle"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "myshuttle-web"
